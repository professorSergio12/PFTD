# Copy to .env for production builds.
# In dev this is unnecessary because Vite proxies /api to the backend
# (see vite.config.js).

# Full base URL of the deployed backend API, e.g.
# VITE_API_URL=https://api.yourdomain.com/api
VITE_API_URL= http://localhost:5000/api